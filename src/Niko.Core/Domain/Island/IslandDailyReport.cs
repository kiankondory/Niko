// ============================================================================
// Niko.Core — IslandDailyReport.cs
// ----------------------------------------------------------------------------
// مسئولیت: نگهداری خروجی مشتق‌شدهٔ گزارش روزانهٔ جزیره شامل شمارش رویدادهای
//           عمومی و مبلغ پس‌انداز همان روز.
// وابستگی‌ها و لایه: Domain در Core؛ بدون وابستگی به UI، SQLite یا پلتفرم.
// نکات تغییر و قیود: این مدل فقط دادهٔ تجمیعی دارد و هیچ شناسه، یادداشت یا
//           ابردادهٔ خصوصی رویداد را به لایهٔ نمایش منتقل نمی‌کند.
// ============================================================================

namespace Niko.Core.Domain.Island;

/// <summary>خلاصهٔ تجمیعی یک روز برای نمایش در جزیره.</summary>
public sealed record IslandDailyReport(
    DateOnly Date,
    int SmokedCount,
    int ResistedCount,
    decimal? SavedAmount);

