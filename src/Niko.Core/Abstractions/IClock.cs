// ============================================================================
// Niko.Core — IClock.cs
// ----------------------------------------------------------------------------
// مسئولیت: ارائهٔ زمان یکنواخت و قابل‌تزریق برای تمام لایه‌ها، به‌طوری که منطق
//           دامنه به زمان واقعی سیستم وابسته نباشد و در تست‌ها قابل شبیه‌سازی باشد.
// وابستگی‌ها و لایه: سکوی Shared Core؛ هیچ وابستگی به UI یا پلتفرم ندارد.
// نکات تغییر و قیود: تمام ذخیره‌سازی داخلی بر پایهٔ UTC انجام می‌شود؛ تبدیل به
//           زمان محلی فقط در مرز نمایش (UI) انجام می‌شود.
// ============================================================================

namespace Niko.Core.Abstractions;

/// <summary>
/// سرویس زمان قابل‌تزریق برای جداسازی دامنه از زمان واقعی و امکان شبیه‌سازی در تست.
/// </summary>
public interface IClock
{
    /// <summary>زمان کنونی به‌صورت UTC.</summary>
    DateTimeOffset UtcNow { get; }
}
