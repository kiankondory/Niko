// ============================================================================
// Niko.Core — EventSource.cs
// ----------------------------------------------------------------------------
// مسئولیت: مشخص کردن منبع ثبت رویداد (موبایل، ساعت هوشمند، ویجت و...).
//           این مقدار به‌همراه EventId برای dedupe و ممیزی استفاده می‌شود.
// وابستگی‌ها و لایه: بخش Events در Core؛ بدون وابستگی به پلتفرم.
// نکات تغییر و قیود: مقادیر عددی پایدار هستند.
// ============================================================================

namespace Niko.Core.Events;

/// <summary>
/// منبع ثبت رویداد. با ورود ساعت‌ها و ویجت‌ها، مقادیر جدید افزوده می‌شوند.
/// </summary>
public enum EventSource
{
    /// <summary>برنامهٔ موبایل.</summary>
    Mobile = 1,

    /// <summary>ساعت هوشمند (Wear OS / watchOS).</summary>
    Wearable = 2,

    /// <summary>ویجت اندروید/آی‌اواس.</summary>
    Widget = 3,
}
