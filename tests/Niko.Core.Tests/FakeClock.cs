// ============================================================================
// Niko.Core.Tests — FakeClock.cs
// ----------------------------------------------------------------------------
// مسئولیت: پیاده‌سازی شبیه‌سازی‌شدهٔ IClock برای تست‌های قطعی و مستقل از زمان واقعی.
// وابستگی‌ها و لایه: لایهٔ تست؛ فقط قرارداد Core را پیاده می‌کند.
// نکات تغییر و قیود: زمان ثابت و قابل تنظیم؛ تست‌ها نباید به زمان واقعی وابسته باشند.
// ============================================================================

using Niko.Core.Abstractions;

namespace Niko.Core.Tests;

/// <summary>
/// ساعت قابل تنظیم برای تست.
/// </summary>
public sealed class FakeClock : IClock
{
    public DateTimeOffset UtcNow { get; set; } = new(2024, 1, 1, 0, 0, 0, TimeSpan.Zero);
}
