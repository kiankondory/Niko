// ============================================================================
// Niko.App — SystemClock.cs
// ----------------------------------------------------------------------------
// مسئولیت: پیاده‌سازی IClock بر پایهٔ زمان واقعی سیستم برای استفاده در تولید.
// وابستگی‌ها و لایه: لایهٔ ارائه (MAUI)؛ قرارداد Core را پیاده می‌کند.
// نکات تغییر و قیود: همیشه UTC برمی‌گرداند؛ تبدیل به محلی در مرز نمایش انجام می‌شود.
// ============================================================================

using Niko.Core.Abstractions;

namespace Niko.Services;

/// <summary>
/// ساعت واقعی سیستم.
/// </summary>
public sealed class SystemClock : IClock
{
    public DateTimeOffset UtcNow => DateTimeOffset.UtcNow;
}
