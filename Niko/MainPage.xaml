<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Niko.ViewModels"
             x:Class="Niko.MainPage"
             x:DataType="vm:MainViewModel">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="20">

            <Label
                Text="{Binding StatusMessage}"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2" />

            <Button
                Text="{Binding SmokedLabel}"
                Command="{Binding SmokedCommand}"
                HorizontalOptions="Fill" />

            <Button
                Text="{Binding ResistedLabel}"
                Command="{Binding ResistedCommand}"
                HorizontalOptions="Fill" />

            <Button
                Text="{Binding CravingLabel}"
                Command="{Binding CravingCommand}"
                HorizontalOptions="Fill" />

            <Button
                Text="{Binding CravingBattleEntryLabel}"
                Clicked="OnCravingBattleClicked"
                HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
