<?xml version="1.0" encoding="utf-8"?>
<!-- مسئولیت: اعلام قابلیت‌های Android و ابزارک؛ منطق دامنه در این فایل نیست.
     وابستگی‌ها و لایه: manifest → MAUI Activity و AppWidgetProvider.
     نکات تغییر و قیود: ابزارک آفلاین است و دادهٔ خصوصی یا مجوز جدید نمی‌خواهد. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true">
		<receiver android:name="com.companyname.niko.NikoWidgetProvider" android:exported="true">
			<intent-filter>
				<action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
			</intent-filter>
			<meta-data android:name="android.appwidget.provider" android:resource="@xml/niko_widget_info" />
		</receiver>
	</application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
